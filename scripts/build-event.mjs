#!/usr/bin/env node
/**
 * `npm run build:event -- --id=<eventId>` — stub (engine plan §4.2).
 * Same rationale as build-addon.mjs: needs the editor-side Special Event content-pack
 * concept (plan §0.4) before a real implementation is meaningful.
 */
console.log('build:event — not implemented yet. See tmp/2D_Editor_ENGINE_PLAN.md §4.2/§0.4.');
