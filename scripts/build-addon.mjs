#!/usr/bin/env node
/**
 * `npm run build:addon -- --id=<addonId>` — stub (engine plan §4.2).
 * Real implementation needs the editor-side "which assets/levels belong to this
 * Addon workspace, overrides vs additions" UI from plan §0.4 first; wiring the CLI
 * before that exists would just be dead code. Command exists now so the interface
 * is settled; fill in once a real addon is being built.
 */
console.log('build:addon — not implemented yet. See tmp/2D_Editor_ENGINE_PLAN.md §4.2/§0.4.');
