# Руководство по версионированию Level Editor

## Обзор

Level Editor использует **централизованную систему управления версиями** с единственным источником истины для всех компонентов проекта.

## Архитектура версионирования

### Единый источник истины

Версия определяется **только в одном месте**:

```javascript
// src/core/LevelEditor.js
export class LevelEditor {
    static VERSION = '2.5.0'; // ← ЕДИНСТВЕННЫЙ ИСТОЧНИК
}
```

### Оптимизированное использование

Версия отображается **минимально**, только где необходимо пользователям:

#### ✅ Места отображения версии:
1. **Консоль при старте редактора**
   - `🚀 Level Editor v2.5.0 - Utility Architecture`
   - Информирует разработчика о запущенной версии

2. **Header главного окна**
   - `2D Level Editor v2.5.0`
   - Постоянно видимо пользователям в интерфейсе

3. **Console команда `version`**
   - По запросу: `> version`
   - Выводит: `2D Level Editor v2.5.0`

#### ❌ Места, где версия была убрана:
- ~~Footer панели ассетов~~ - избыточно
- ~~Title страницы с версией~~ - достаточно базового названия
- ~~Различные хардкод fallback'и~~ - заменены на динамическое получение

## Процедура обновления версии

### Шаги обновления:

1. **Определить тип изменения и новую версию**
   - **Patch (X.Y.Z+1)** - исправления багов, мелкие изменения
   - **Minor (X.Y+1.0)** - новые функции, обратно совместимые изменения
   - **Major (X+1.0.0)** - breaking changes, кардинальные изменения

2. **Обновить основную версию**
   ```javascript
   // src/core/LevelEditor.js
   static VERSION = '2.2.1'; // новая версия (patch)
   static VERSION = '2.3.0'; // новая версия (minor)
   static VERSION = '3.0.0'; // новая версия (major)
   ```

3. **Синхронизировать package.json**
   ```json
   {
     "version": "2.2.1"  // должна совпадать с LevelEditor.VERSION
   }
   ```

4. **Все остальное обновится автоматически**
   - Логирование в консоли
   - Отображение в header'е
   - Console команды

### Правила для Git коммитов:

- **Patch изменения**: `git commit -m "v2.2.1: Исправление бага с автоудалением групп"`
- **Minor изменения**: `git commit -m "v2.3.0: Добавлена новая функциональность X"`
- **Major изменения**: `git commit -m "v3.0.0: Кардинальные изменения архитектуры"`

### Семантическое версионирование

Level Editor следует [SemVer](https://semver.org/lang/ru/) с правилом сброса младших версий:

- **Major (X.0.0)** - breaking changes в архитектуре
- **Minor (X.Y.0)** - новые features, обратно совместимые
- **Patch (X.Y.Z)** - исправления багов и мелкие изменения

#### Правило сброса версий:
- При повышении **Major** версии → сбрасываем Minor и Patch в 0
- При повышении **Minor** версии → сбрасываем Patch в 0
- **Patch** версия увеличивается для мелких изменений

#### Примеры:
- `2.1.0 → 2.1.1` - исправление бага (patch)
- `2.1.0 → 2.2.0` - новая функциональность (minor, patch сброшен в 0)
- `2.1.0 → 3.0.0` - кардинальные изменения архитектуры (major, minor и patch сброшены в 0)
- `2.1.5 → 2.2.0` - новая функциональность (minor, patch сброшен с 5 в 0)
- `2.1.5 → 3.0.0` - breaking changes (major, minor и patch сброшены в 0)

## Принципы оптимизации

### ✅ Что мы делаем:
- **Единственный источник истины** для версии
- **Минимальное отображение** - только где нужно пользователям
- **Динамическое получение** версии во всех местах
- **Отсутствие дублирования** в коде

### ❌ Что мы избегаем:
- Хардкод версий в разных местах
- Избыточное отображение версии в UI
- Несинхронизированные версии между компонентами
- Fallback на устаревшие версии

## Техническая реализация

### Динамическое получение версии

```javascript
// Правильно - динамическое получение
const version = window.editor ? window.editor.constructor.VERSION : 'unknown';

// Неправильно - хардкод
const version = '2.1.0'; // ❌ не делать так
```

### Обновление UI элементов

```javascript
// Метод автоматически обновляет header
updateVersionInfo() {
    const headerVersionElement = document.getElementById('header-version-info');
    if (headerVersionElement) {
        headerVersionElement.textContent = `2D Level Editor v${LevelEditor.VERSION}`;
    }
}
```

## Мониторинг и проверка

### Проверка консистентности версий:

```bash
# Проверить version в package.json
grep '"version"' package.json

# Найти VERSION в коде
grep -r "VERSION.*=" src/core/LevelEditor.js
```

### Тестирование версионирования:

1. Запустить редактор
2. Проверить логирование в консоли браузера
3. Проверить отображение в header'е
4. Выполнить команду `version` в консоли редактора

## История изменений политики версионирования

- **v3.13.1** - Исправления перемещения объектов и UI
  - Исправлено перемещение объектов во вложенных группах - добавлена рекурсивная проверка принадлежности к группам
  - Исправлена индексация объектов при создании групп - дочерние объекты добавляются в индекс с правильным topLevelParent
  - Почищены клики на слоях в LayersPanel - обработчики устанавливаются корректно при рендере
  - Исправлена логика shift+click в OutlinerPanel - добавлен anchor point для выделения диапазона
  - Почищено middle mouse scrolling - рефакторинг ScrollUtils для предотвращения конфликтов между панелями

- **v3.12.0** - Эффект параллакса слоев (расширенная архитектура)
  - Отдельный модуль ParallaxRenderer - вынесена вся логика параллакса в dedicated класс
  - Исключение нулевых коэффициентов - слои с parallaxOffset = 0 не участвуют в параллаксе
  - Исправленное выделение - объекты правильно выделяются с учетом параллакс смещения
  - Горячая клавиша P - быстрый тоггл режима параллакса
  - Настройка коэффициентов - числовые поля в панели слоев для управления скоростью движения
  - Точка отсчета камеры - кнопка "Set Camera Start Position" для установки референсной позиции
  - Реалтайм эффект - объекты двигаются с разной скоростью при перемещении камеры
  - Наследование по слоям - каждый слой может иметь индивидуальный коэффициент параллакса

- **v3.11.1** - Исправление бага дублирования множественных объектов
  - Исправлена критическая проблема с дублированием множества выбранных объектов (Shift+D)
  - Переработана логика позиционирования в DuplicateOperations.js для корректного расчета относительных смещений
  - Объекты теперь сохраняют свои относительные позиции при дублировании вместо размещения в одной точке
  - Улучшена стабильность операций дублирования для групп и одиночных объектов

- **v3.4.1** - Улучшенная система привязки объектов к слоям
  - Полная переработка метода moveSelectedObjectsToLayer() для корректной обработки всех выбранных объектов
  - Добавлены новые методы assignSelectedObjectsToLayer() и processObjectForLayerAssignment()
  - Улучшенная обработка вложенных объектов в группах с правильным наследованием layerId
  - Добавлены проверки существования renderOperations для предотвращения ошибок инициализации
  - Добавлена категория LAYER в систему логирования Logger для улучшенной отладки
  - Исправлены все ошибки "Cannot read properties of undefined" в операциях со слоями

- **v2.5.0** - View меню и улучшения StateManager
  - Добавлено View меню с переключением режимов отображения
  - Grid - переключение отображения сетки
  - Game Mode - скрытие UI панелей для игрового режима
  - Snap To Grid - привязка объектов к сетке
  - Object Boundaries - отображение границ объектов
  - Object Collisions - отображение коллизий объектов
  - Исправлен StateManager для поддержки вложенных ключей (view.grid, canvas.showGrid)
  - Добавлены начальные состояния для view и canvas объектов
  - Исправлены предупреждения браузера о form field элементах

- **v2.4.6** - Полное исправление удаления групп с вложенными детьми
  - Полностью переписана логика deleteSelectedObjects() для корректного удаления групп
  - Исправлена проблема с детьми групп, находящимися во вложенных массивах
  - Добавлена рекурсивная обработка всех уровней вложенности групп
  - Добавлена правильная очистка массивов детей в оставшихся группах
  - Группы теперь удаляются полностью со всем содержимым, включая вложенные группы
  - Улучено логирование для отладки процесса удаления

- **v2.4.5** - Исправление удаления групп
  - Изменено поведение deleteSelectedObjects() - теперь группы удаляются полностью с детьми
  - Убрана логика переноса детей на основной уровень при удалении группы
  - Группы теперь удаляются со всем содержимым, а не разгруппировываются
  - Улучшена документация по удалению объектов и групп

- **v2.4.4** - Обновление документации системы слоев
  - Обновлена COMPREHENSIVE_API_REFERENCE.md с новыми методами слоев
  - Добавлена документация Layer класса и LayersPanel
  - Обновлено описание методов Level для работы со слоями
  - Уточнены описания GroupOperations с учетом назначения в Main слой

- **v2.4.3** - Улучшение системы слоев и их устойчивости
  - Исправлена система назначения Main слоя для новых объектов
  - Добавлено постоянное хранение ID Main слоя независимо от перестановки слоев
  - Реализована корректная сериализация/десериализация состояния слоев
  - Улучшена система подсчета объектов в слоях с учетом групп
  - Исправлена привязка ассетов к Main слою по ID, а не по позиции в массиве

- **v3.8.0** - Принудительное наследование layerId для групп
  - Реализована прямая зависимость: layerId группы = layerId всех детей
  - Принудительное наследование при всех операциях с группами
  - Обновлен подсчет объектов в слоях с учетом вложенных объектов
  - Исправлена проблема с изменением слоя по кнопке для вложенных объектов

- **v2.4.2** - Исправления системы слоев
  - Исправлена проблема с позиционированием слоев в интерфейсе
  - Улучшена стабильность ID основного слоя при перестановке слоев
  - Добавлена защита от изменения ID слоя при его перемещении

- **v2.4.1** - Исправления подсчета объектов в слоях
  - Исправлено назначение Main слоя по имени вместо позиции в массиве
  - Добавлен метод `getMainLayerId()` для получения ID основного слоя
  - Улучшена система подсчета объектов в слоях с учетом групп верхнего уровня

- **v2.4.0** - Улучшения системы слоев
  - Добавлена система назначения объектов в Main слой по ID
  - Улучшена привязка ассетов к слоям независимо от их позиции
  - Исправлена система подсчета объектов в слоях

- **v2.3.2** - Исправления подсветки дублирования
  - Исправлена проблема с отрисовкой подсветки дублируемых объектов
  - Добавлена специализированная отрисовка для дублируемых групп
  - Улучшена подсветка иерархии вложенных групп при дублировании

- **v2.3.1** - Предыдущие улучшения архитектуры

- **v2.1.0** - Внедрена оптимизированная система версионирования
  - Убрано дублирование отображения версии
  - Устранены хардкод fallback'и
  - Создано единое место управления версией

- **v2.0.0** - Utility Architecture с базовой централизованной версией

## Заключение

Текущая система версионирования обеспечивает:
- **Простоту обслуживания** - изменения в одном месте
- **Консистентность** - все компоненты показывают одну версию  
- **Минимализм** - версия отображается только где нужно
- **Надежность** - нет устаревших хардкод значений

Эта система позволяет легко управлять версиями без дублирования кода и избыточности в интерфейсе.
